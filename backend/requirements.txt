# =============================================================================
# 🐍 Snake Game Backend — Зависимости
# =============================================================================

# FastAPI — современный веб-фреймворк для создания API
fastapi==0.109.2

# Uvicorn — ASGI сервер для запуска FastAPI
uvicorn[standard]==0.27.1

# SQLAlchemy — ORM для работы с базой данных
sqlalchemy==2.0.25

# Pydantic Settings — конфигурация через переменные окружения
pydantic-settings==2.1.0

# Python-dotenv — загрузка .env файла
python-dotenv==1.0.1

# Asyncpg — асинхронный драйвер для PostgreSQL (production)
asyncpg==0.29.0

# Aiosqlite — асинхронный драйвер для SQLite (локальная разработка)
aiosqlite==0.19.0

# Greenlet — нужен для асинхронной работы SQLAlchemy
greenlet==3.0.3
