# 🐍 Snake Game — Правила для AI-агентов

## Описание проекта
Браузерная игра "Змейка" с двумя сервисами:
- **Backend**: Python + FastAPI + SQLite
- **Frontend**: React + Vite

## Архитектура

### Backend (Python)
- Фреймворк: FastAPI
- База данных: SQLite (файл `snake.db`)
- ORM: SQLAlchemy
- Конфигурация: Pydantic Settings

### Frontend (React)
- Сборщик: Vite
- UI библиотека: React 18+
- Анимации: CSS + Framer Motion
- HTTP клиент: fetch API

## Правила кодирования

### Python (Backend)
1. **Type hints** обязательны везде
2. **Docstrings** для всех функций и классов
3. Использовать **async/await** для асинхронных операций
4. Импорты по порядку: stdlib → third-party → local
5. Именование:
   - Классы: `PascalCase`
   - Функции/переменные: `snake_case`
   - Константы: `UPPER_SNAKE_CASE`

### JavaScript/React (Frontend)
1. Использовать **функциональные компоненты** с хуками
2. Именование:
   - Компоненты: `PascalCase` (Game.jsx, Menu.jsx)
   - Функции/переменные: `camelCase`
   - CSS классы: `kebab-case`
3. Деструктуризация props
4. Комментарии на русском языке

## Структура файлов

### Backend
```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py           # FastAPI приложение
│   ├── database.py       # SQLite подключение
│   ├── models.py         # SQLAlchemy модели
│   ├── schemas.py        # Pydantic схемы
│   └── routers/
│       ├── game.py       # /api/game/*
│       └── leaderboard.py # /api/leaderboard
├── settings.py           # Pydantic Settings
└── requirements.txt
```

### Frontend
```
frontend/
├── src/
│   ├── components/       # React компоненты
│   ├── hooks/            # useGame, useLeaderboard
│   ├── services/         # API клиент
│   ├── styles/           # CSS
│   ├── App.jsx
│   └── main.jsx
└── package.json
```

## API Endpoints

| Method | Endpoint | Описание |
|--------|----------|----------|
| POST | `/api/game/result` | Сохранить результат игры |
| GET | `/api/leaderboard` | Таблица лидеров (TOP-10) |
| GET | `/api/stats` | Статистика игрока |
| GET | `/api/health` | Проверка работоспособности |

## Игровая механика

### Управление
- Стрелки (↑↓←→) или WASD
- Пробел/P — пауза
- Enter — рестарт после Game Over

### Правила
- Змейка растёт при поедании еды
- Скорость увеличивается каждые 5 очков
- Игра заканчивается при столкновении со стеной или собой
- Типы еды:
  - 🍎 Обычная: +1 очко
  - ⭐ Бонусная: +3 очка (появляется редко, исчезает через 5 сек)

## Дизайн

### Цветовая схема
- Фон: тёмный (#0f0f23)
- Змейка: градиент зелёный (#22c55e → #16a34a)
- Еда: красный (#ef4444)
- Бонус: золотой (#fbbf24)
- Акцент: неоновый (#00ff88)

### Анимации
- Плавное движение змейки
- Пульсация еды
- Эффект при поедании
- Анимация Game Over
- Переходы между экранами

## Тестирование

Перед коммитом:
```bash
# Backend
cd backend
pytest -v

# Frontend
cd frontend
npm run lint
```

## Комментарии в коде
- Писать на **русском языке**
- Объяснять "почему", а не "что"
- Документировать сложную логику

## Definition of Done
- [ ] Код работает без ошибок
- [ ] Есть docstrings/комментарии
- [ ] Нет lint ошибок
- [ ] Протестировано вручную
