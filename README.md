# Project Snake 🐍

> Описание проекта будет добавлено после определения задачи.

## 🚀 Быстрый старт

```bash
# 1. Клонировать репозиторий
git clone <repo_url>
cd projectsnake

# 2. Создать виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# .venv\Scripts\activate   # Windows

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Настроить переменные окружения
cp .env.example .env
# Отредактировать .env с реальными значениями

# 5. Запустить приложение
# (команда будет добавлена)
```

## 📁 Структура проекта

```
projectsnake/
├── .cursorrules              # Правила для Cursor AI агентов
├── .env                      # Переменные окружения (не коммитить!)
├── .env.example              # Пример переменных окружения
├── .gitignore                # Игнорируемые файлы
├── .flake8                   # Конфигурация линтера
├── README.md                 # Этот файл
├── settings.py               # Pydantic Settings
│
├── app/                      # Основной код приложения
│   └── __init__.py
│
├── tests/                    # Тесты
│   ├── unit/
│   ├── integration/
│   └── e2e/
│
├── scripts/                  # Вспомогательные скрипты
│
├── docs/                     # Документация
│   ├── changelogs/           # Changelog'и агентов
│   └── production/           # Документация для внешних разработчиков
│
├── temporary/                # Временные скрипты/эксперименты
│
├── Docs/                     # Исходная документация (dev-playbook, nakama_api)
│
├── docker-compose.yml        # Docker Compose для production
├── docker-compose-dev.yml    # Docker Compose для разработки
└── Dockerfile                # Основной Dockerfile
```

## 🔑 Доступные интеграции

Проект имеет готовые ключи для:

| Категория | Сервисы |
|-----------|---------|
| **БД** | PostgreSQL (Yandex Cloud), Supabase |
| **AI** | OpenAI, OpenRouter |
| **Google** | Service Account + Domain-wide delegation |
| **Транскрибация** | SynopsisAI, SpeakAI, Nexara |
| **Интеграции** | n8n, PII Detector, PACHCA, Zoom |
| **Email** | SMTP |
| **API** | Nakama API (spellit.ai) |

## 📖 Документация

- `Docs/dev-playbook.md` — Правила разработки
- `Docs/nakama_api_*.md` — Документация Nakama API

## 🧪 Тестирование

```bash
# Запустить все тесты
pytest -v

# Только unit тесты
pytest tests/unit/ -v

# С покрытием
pytest --cov=app tests/
```

## 🐳 Docker

```bash
# Разработка
docker-compose -f docker-compose-dev.yml up -d

# Production
docker-compose up -d --build
```

---

*Документация будет дополнена после определения задачи проекта.*
