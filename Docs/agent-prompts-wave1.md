# 🌊 Волна 1: Промпты для агентов

> **Волна 1** — параллельная разработка, 5 агентов
> **Дата:** 2026-01-22

---

## Как запускать агентов

1. Открой **новый чат** в Cursor (Cmd+L или Ctrl+L)
2. Скопируй промпт агента целиком
3. Отправь и дай агенту работать
4. После завершения — проверь changelog в `docs/changelogs/`

**Можно запускать все 5 агентов ПАРАЛЛЕЛЬНО** — они работают в разных зонах.

---

## 🤖 Агент 1.1: Backend Admin API

```
## [Admin API] Agent 1.1 — Backend для управления кампаниями

### Контекст
Ты — AI-агент в проекте Snake Game. Это игра-змейка с геймифицированными промокодами.
Твоя задача: создать Backend API для управления кампаниями (промокоды, настройки, брендинг).

### ⚠️ ОБЯЗАТЕЛЬНО СНАЧАЛА
1. Прочитай `Docs/monetization-plan.md` — общий план монетизации
2. Прочитай `.cursorrules` — правила кодирования проекта
3. Изучи существующий код в `backend/app/` — структуру, стиль, паттерны
4. **Только после этого приступай к выполнению!**

### Твоя зона ответственности
Ты работаешь ТОЛЬКО с этими файлами:

```
backend/
├── app/
│   ├── routers/
│   │   └── admin.py          ← СОЗДАТЬ
│   ├── schemas/
│   │   └── admin.py          ← СОЗДАТЬ (если нет папки schemas — создай)
│   ├── services/
│   │   └── campaign.py       ← СОЗДАТЬ (если нет папки services — создай)
│   └── models.py             ← ИЗМЕНИТЬ (добавить модель Campaign)
├── tests/
│   └── test_admin.py         ← СОЗДАТЬ
└── docs/changelogs/
    └── wave-1-agent-1.md     ← СОЗДАТЬ в конце
```

### НЕ ТРОГАЙ
- `backend/app/routers/game.py` — это зона основной игры
- `backend/app/routers/leaderboard.py` — это зона лидерборда
- `frontend/` — это зона других агентов
- `backend/app/main.py` — только добавь импорт роутера, не меняй логику

### Задачи

1. [ ] **Модель Campaign** в `models.py`:
   - id: int (PK)
   - slug: str (unique) — для URL
   - name: str — название кампании
   - promo_code: str — промокод для показа
   - promo_message: str — шаблон сообщения (с {playerName})
   - primary_color: str — HEX цвет (#00ff88)
   - secondary_color: str — HEX цвет
   - logo_url: str (optional) — URL логотипа
   - is_active: bool — активна ли кампания
   - created_at: datetime
   - updated_at: datetime

2. [ ] **Pydantic схемы** в `schemas/admin.py`:
   - CampaignCreate
   - CampaignUpdate
   - CampaignResponse
   - CampaignListResponse

3. [ ] **CRUD сервис** в `services/campaign.py`:
   - create_campaign()
   - get_campaign_by_id()
   - get_campaign_by_slug()
   - update_campaign()
   - delete_campaign()
   - list_campaigns()

4. [ ] **API эндпоинты** в `routers/admin.py`:
   - POST /api/admin/campaigns — создать
   - GET /api/admin/campaigns — список всех
   - GET /api/admin/campaigns/{id} — получить по ID
   - PUT /api/admin/campaigns/{id} — обновить
   - DELETE /api/admin/campaigns/{id} — удалить
   - GET /api/campaigns/{slug} — ПУБЛИЧНЫЙ эндпоинт для фронта

5. [ ] **Подключить роутер** в `main.py`

6. [ ] **Тесты** в `tests/test_admin.py`:
   - test_create_campaign
   - test_get_campaign
   - test_update_campaign
   - test_delete_campaign
   - test_get_campaign_by_slug

7. [ ] **Changelog** в `docs/changelogs/wave-1-agent-1.md`

### Definition of Done
- [ ] Все эндпоинты работают (проверить через /docs)
- [ ] Тесты проходят
- [ ] Код соответствует стилю проекта (docstrings, type hints)
- [ ] Changelog создан

### Технические требования
- Используй async/await везде
- Type hints обязательны
- Docstrings на русском языке
- Следуй паттернам существующего кода

### Пример ответа API

```json
{
  "id": 1,
  "slug": "yandex-travel-2024",
  "name": "Яндекс Путешествия — Новый Год",
  "promo_code": "PF-VIGODA-AH37X",
  "promo_message": "{playerName}, лови -20% в Я.Путешествиях! Код: PF-VIGODA-AH37X",
  "primary_color": "#00ff88",
  "secondary_color": "#0a0a1a",
  "logo_url": "https://example.com/logo.png",
  "is_active": true,
  "created_at": "2026-01-22T12:00:00Z",
  "updated_at": "2026-01-22T12:00:00Z"
}
```
```

---

## 🤖 Агент 1.2: Frontend Админка

```
## [Admin UI] Agent 1.2 — Админка для управления кампаниями

### Контекст
Ты — AI-агент в проекте Snake Game. Это игра-змейка с геймифицированными промокодами.
Твоя задача: создать UI админки для управления кампаниями.

### ⚠️ ОБЯЗАТЕЛЬНО СНАЧАЛА
1. Прочитай `Docs/monetization-plan.md` — общий план монетизации
2. Прочитай `.cursorrules` — правила кодирования проекта
3. Изучи существующий код в `frontend/src/` — структуру, стиль, компоненты
4. **Только после этого приступай к выполнению!**

### Твоя зона ответственности

```
frontend/src/
├── pages/
│   └── Admin/                ← СОЗДАТЬ
│       ├── index.js
│       ├── Admin.jsx
│       ├── Admin.module.css
│       ├── CampaignForm.jsx
│       ├── CampaignForm.module.css
│       ├── CampaignList.jsx
│       └── CampaignList.module.css
├── services/
│   └── adminApi.js           ← СОЗДАТЬ
└── components/
    └── UI/
        └── ColorPicker/      ← СОЗДАТЬ
            ├── index.js
            ├── ColorPicker.jsx
            └── ColorPicker.module.css
```

### НЕ ТРОГАЙ
- `frontend/src/App.jsx` — это изменит Агент 2.1
- `frontend/src/hooks/useGame.js` — это изменит Агент 2.1
- `frontend/src/components/Game/` — основная игра
- `backend/` — это зона Агента 1.1

### Задачи

1. [ ] **API клиент** `services/adminApi.js`:
   - createCampaign(data)
   - getCampaigns()
   - getCampaign(id)
   - updateCampaign(id, data)
   - deleteCampaign(id)
   
   Пока используй моковые данные, реальный API подключит Агент 2.1

2. [ ] **Компонент ColorPicker**:
   - Выбор цвета через input[type="color"]
   - Отображение HEX значения
   - Предпросмотр цвета

3. [ ] **Страница Admin.jsx**:
   - Заголовок "Управление кампаниями"
   - Кнопка "Создать кампанию"
   - Список существующих кампаний (CampaignList)
   - Модальное окно с формой (CampaignForm)

4. [ ] **Компонент CampaignList.jsx**:
   - Таблица/карточки кампаний
   - Статус (активна/неактивна)
   - Кнопки: редактировать, удалить, копировать ссылку
   - Пустое состояние "Нет кампаний"

5. [ ] **Компонент CampaignForm.jsx**:
   - Поля: название, slug, промокод, сообщение, цвета, URL лого
   - Валидация (обязательные поля)
   - Кнопки: сохранить, отмена
   - Режимы: создание / редактирование

6. [ ] **Стили** — следуй дизайн-системе проекта:
   - Используй CSS переменные из `styles/index.css`
   - Тёмная тема
   - Адаптивность для мобильных

7. [ ] **Changelog** в `docs/changelogs/wave-1-agent-2.md`

### Definition of Done
- [ ] Страница админки рендерится без ошибок
- [ ] Форма валидирует данные
- [ ] ColorPicker работает
- [ ] Моковые данные отображаются
- [ ] Адаптивность для мобильных
- [ ] Changelog создан

### Дизайн
- Тёмная тема (как основная игра)
- Акцентный цвет: #00ff88
- Используй существующие CSS переменные
- Минималистичный UI
```

---

## 🤖 Агент 1.3: Лендинг для бизнеса

```
## [Landing] Agent 1.3 — Лендинг для B2B продаж

### Контекст
Ты — AI-агент в проекте Snake Game. Это игра-змейка с геймифицированными промокодами.
Твоя задача: создать продающий лендинг для привлечения бизнес-клиентов.

### ⚠️ ОБЯЗАТЕЛЬНО СНАЧАЛА
1. Прочитай `Docs/monetization-plan.md` — общий план монетизации
2. Изучи основную игру: https://frontend-drab-ten-89.vercel.app
3. **Только после этого приступай к выполнению!**

### Твоя зона ответственности

```
landing/                       ← СОЗДАТЬ (новая папка в корне проекта!)
├── index.html
├── styles.css
├── script.js
├── assets/
│   └── (скриншоты добавятся позже)
└── README.md
```

### НЕ ТРОГАЙ
- `frontend/` — основной фронтенд игры
- `backend/` — бэкенд
- Любые другие папки

### Задачи

1. [ ] **Hero-секция**:
   - Заголовок: "Геймифицируйте ваши промокоды"
   - Подзаголовок: "Конверсия в 3-5 раз выше обычных промокодов"
   - CTA кнопка: "Попробовать демо" → ссылка на игру
   - Embed iframe с демо-игрой (опционально)

2. [ ] **Секция "Как это работает"** (3 шага):
   - 1. Создайте кампанию — настройте промокод и бренд
   - 2. Поделитесь ссылкой — в email, соцсетях, на сайте
   - 3. Получайте результат — пользователи играют и получают промокод

3. [ ] **Секция "Примеры использования"**:
   - E-commerce (скидки на товары)
   - Сервисы подписки (промо-период)
   - Email-маркетинг (интерактивные рассылки)
   - Мероприятия (геймификация стендов)

4. [ ] **Секция "Тарифы"**:
   | Тариф | Цена | Что включено |
   |-------|------|--------------|
   | Старт | 30K ₽ | 1 кампания, базовые настройки |
   | Бизнес | 80K ₽ | Кастомный дизайн, аналитика |
   | Подписка | 15K ₽/мес | Безлимит кампаний, приоритетная поддержка |

5. [ ] **Секция "Почему мы"**:
   - Быстрый запуск (1 день)
   - Полная кастомизация
   - Мобильная адаптация
   - Детальная аналитика

6. [ ] **Форма заявки**:
   - Поля: имя, email, компания, комментарий
   - Интеграция с Tally.so или просто mailto: ссылка
   - Или: кнопка "Написать в Telegram"

7. [ ] **Footer**:
   - Контакты
   - Ссылки на соцсети
   - Copyright

8. [ ] **README.md** с инструкцией по деплою

9. [ ] **Changelog** в `docs/changelogs/wave-1-agent-3.md`

### Definition of Done
- [ ] Страница загружается локально (открыть index.html)
- [ ] Адаптивность для мобильных
- [ ] Все секции присутствуют
- [ ] CTA кнопки работают
- [ ] Changelog создан

### Дизайн
- Тёмная тема (#0f0f23 фон)
- Акцентный цвет: #00ff88 (зелёный неон)
- Современный минималистичный стиль
- Анимации при скролле (опционально)
- Шрифты: системные или Google Fonts (Space Grotesk, Inter)

### Технические требования
- Чистый HTML/CSS/JS (без фреймворков)
- Оптимизация изображений
- Семантическая вёрстка
- SEO метатеги
```

---

## 🤖 Агент 1.4: Embed-виджет

```
## [Embed] Agent 1.4 — Встраиваемый виджет игры

### Контекст
Ты — AI-агент в проекте Snake Game. Это игра-змейка с геймифицированными промокодами.
Твоя задача: создать embed-версию игры для встраивания на внешние сайты.

### ⚠️ ОБЯЗАТЕЛЬНО СНАЧАЛА
1. Прочитай `Docs/monetization-plan.md` — общий план монетизации
2. Изучи код игры в `frontend/src/components/Game/`
3. Изучи хук `frontend/src/hooks/useGame.js`
4. **Только после этого приступай к выполнению!**

### Твоя зона ответственности

```
frontend/
├── src/
│   └── embed/                ← СОЗДАТЬ
│       ├── EmbedGame.jsx     # Упрощённая версия игры
│       ├── EmbedGame.module.css
│       ├── embed-entry.jsx   # Entry point для Vite
│       └── README.md         # Документация по использованию
├── vite.config.embed.js      ← СОЗДАТЬ (отдельный конфиг)
└── package.json              ← ИЗМЕНИТЬ (добавить скрипт build:embed)
```

### НЕ ТРОГАЙ
- `frontend/src/App.jsx` — основное приложение
- `frontend/src/components/Game/Game.jsx` — можешь копировать, но не изменять
- `backend/` — бэкенд

### Задачи

1. [ ] **EmbedGame.jsx** — упрощённая версия игры:
   - Без меню (сразу игра)
   - Без лидерборда
   - Без ввода имени (имя из параметров)
   - Компактный UI
   - Настройки через props/URL параметры:
     - `playerName` — имя игрока
     - `promoCode` — промокод
     - `promoMessage` — шаблон сообщения
     - `primaryColor` — основной цвет
     - `secondaryColor` — фоновый цвет

2. [ ] **embed-entry.jsx** — entry point:
   - Читает параметры из URL или data-атрибутов
   - Рендерит EmbedGame
   - Отправляет postMessage при завершении игры

3. [ ] **vite.config.embed.js**:
   - Отдельный билд для embed
   - Output: `dist-embed/`
   - Минимальный бандл

4. [ ] **Обновить package.json**:
   - Добавить скрипт: `"build:embed": "vite build --config vite.config.embed.js"`

5. [ ] **PostMessage API** для интеграции:
   ```javascript
   // При завершении игры
   window.parent.postMessage({
     type: 'snake-game:complete',
     data: {
       score: 42,
       victory: true,
       promoCode: 'SALE20',
       revealedText: 'Полный текст промокода'
     }
   }, '*');
   ```

6. [ ] **README.md** с примерами использования:
   ```html
   <!-- Вариант 1: iframe -->
   <iframe 
     src="https://snake.example.com/embed?promo=SALE20&player=User"
     width="400" 
     height="500"
   ></iframe>
   
   <!-- Вариант 2: script (опционально) -->
   <div id="snake-game" data-promo="SALE20"></div>
   <script src="https://snake.example.com/embed.js"></script>
   ```

7. [ ] **Changelog** в `docs/changelogs/wave-1-agent-4.md`

### Definition of Done
- [ ] `npm run build:embed` создаёт бандл
- [ ] Embed версия работает в iframe
- [ ] Настройки применяются из URL параметров
- [ ] postMessage отправляется при завершении
- [ ] Бандл < 150KB
- [ ] Changelog создан

### Технические требования
- Переиспользуй логику из useGame.js
- Минимизируй зависимости
- Поддержка IE11 не нужна
```

---

## 🤖 Агент 1.5: Аналитика

```
## [Analytics] Agent 1.5 — Система аналитики

### Контекст
Ты — AI-агент в проекте Snake Game. Это игра-змейка с геймифицированными промокодами.
Твоя задача: создать систему аналитики для отслеживания игровых событий.

### ⚠️ ОБЯЗАТЕЛЬНО СНАЧАЛА
1. Прочитай `Docs/monetization-plan.md` — общий план монетизации
2. Изучи существующий код в `backend/app/`
3. Изучи `frontend/src/hooks/useGame.js`
4. **Только после этого приступай к выполнению!**

### Твоя зона ответственности

**Backend:**
```
backend/
├── app/
│   ├── routers/
│   │   └── analytics.py      ← СОЗДАТЬ
│   ├── schemas/
│   │   └── analytics.py      ← СОЗДАТЬ
│   └── models.py             ← ИЗМЕНИТЬ (добавить GameEvent)
└── tests/
    └── test_analytics.py     ← СОЗДАТЬ
```

**Frontend:**
```
frontend/src/
├── services/
│   └── analyticsApi.js       ← СОЗДАТЬ
└── hooks/
    └── useAnalytics.js       ← СОЗДАТЬ
```

### НЕ ТРОГАЙ
- `backend/app/routers/game.py`
- `backend/app/routers/leaderboard.py`
- `frontend/src/hooks/useGame.js` — это изменит Агент 2.2
- `frontend/src/components/` — основные компоненты

### Задачи

#### Backend

1. [ ] **Модель GameEvent** в `models.py`:
   - id: int (PK)
   - campaign_id: int (FK, optional) — к какой кампании относится
   - session_id: str — уникальный ID сессии игры
   - event_type: str — тип события
   - event_data: JSON — дополнительные данные
   - created_at: datetime

2. [ ] **Типы событий**:
   - `game_start` — начало игры
   - `game_pause` — пауза
   - `game_resume` — продолжение
   - `food_eaten` — съедена еда (data: {score, length})
   - `bonus_eaten` — съеден бонус (data: {points, phase})
   - `game_over` — конец игры (data: {score, length, duration, revealed_chars})
   - `victory` — победа (data: {score, length, duration})

3. [ ] **Pydantic схемы** в `schemas/analytics.py`:
   - GameEventCreate
   - GameEventResponse
   - CampaignStatsResponse

4. [ ] **API эндпоинты** в `routers/analytics.py`:
   - POST /api/analytics/event — записать событие
   - GET /api/analytics/campaign/{id}/stats — статистика кампании

5. [ ] **Статистика кампании** должна включать:
   - Всего игр
   - Средний счёт
   - Среднее время игры
   - % дошедших до N символов (для каждого N)
   - % побед (дошли до конца)
   - Распределение по дням

#### Frontend

6. [ ] **API клиент** `analyticsApi.js`:
   - trackEvent(eventType, eventData, campaignId, sessionId)

7. [ ] **Хук useAnalytics.js**:
   ```javascript
   const { trackEvent, sessionId } = useAnalytics(campaignId);
   
   // Использование:
   trackEvent('game_start');
   trackEvent('food_eaten', { score: 5, length: 8 });
   trackEvent('game_over', { score: 42, duration: 120 });
   ```

8. [ ] **Changelog** в `docs/changelogs/wave-1-agent-5.md`

### Definition of Done
- [ ] Эндпоинт записи событий работает
- [ ] Эндпоинт статистики возвращает агрегированные данные
- [ ] Хук useAnalytics готов к интеграции
- [ ] Тесты проходят
- [ ] Changelog создан

### Пример ответа статистики

```json
{
  "campaign_id": 1,
  "total_games": 1542,
  "total_victories": 89,
  "victory_rate": 5.77,
  "avg_score": 23.4,
  "avg_duration_sec": 145.2,
  "avg_revealed_chars": 18.7,
  "completion_funnel": [
    { "chars": 10, "percentage": 78.5 },
    { "chars": 20, "percentage": 45.2 },
    { "chars": 30, "percentage": 22.1 },
    { "chars": 40, "percentage": 8.3 },
    { "chars": 50, "percentage": 5.8 }
  ],
  "daily_stats": [
    { "date": "2026-01-20", "games": 234, "victories": 12 },
    { "date": "2026-01-21", "games": 456, "victories": 28 }
  ]
}
```
```

---

## 📋 Чеклист запуска Волны 1

После запуска всех агентов:

- [ ] Агент 1.1 создал `docs/changelogs/wave-1-agent-1.md`
- [ ] Агент 1.2 создал `docs/changelogs/wave-1-agent-2.md`
- [ ] Агент 1.3 создал `docs/changelogs/wave-1-agent-3.md`
- [ ] Агент 1.4 создал `docs/changelogs/wave-1-agent-4.md`
- [ ] Агент 1.5 создал `docs/changelogs/wave-1-agent-5.md`

**После проверки МОЗГОМ → переходим к Волне 2**
